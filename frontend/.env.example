# Base URL of the WordPress REST API. Leave empty to use same-origin /wp-json
# (recommended in production behind Nginx; the dev server proxies it).
VITE_API_BASE=
# Dev-only: where the vite proxy forwards /wp-json requests
VITE_WP_PROXY_TARGET=http://localhost:8080
